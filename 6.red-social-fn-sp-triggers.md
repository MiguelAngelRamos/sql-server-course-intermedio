```sql
CREATE FUNCTION fn_ObtenerConteoLikes
(
    @PublicacionID INT
)
RETURNS INT
AS
BEGIN
    DECLARE @Conteo INT;
    SELECT @Conteo = COUNT(*)
    FROM Likes
    WHERE PublicacionID = @PublicacionID;
    RETURN @Conteo;
END;
```